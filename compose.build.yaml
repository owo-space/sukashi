# Override that builds the image locally instead of pulling from GHCR.
# Usage:
#   docker compose -f compose.yaml -f compose.build.yaml up -d --build
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: sukashi:local
    pull_policy: build
